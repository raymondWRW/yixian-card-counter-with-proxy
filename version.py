"""
Main app (YiXian Counter) version. Bump this before each main-app build,
then upload the produced exe to the matching Gitee Release tag and update
dist_share/version.json with the new version + SHA256.

Released as Gitee tag `main-v<VERSION>` to keep main and lite releases in
the same repo without colliding (lite uses `lite-v<VERSION>`).
"""
VERSION = "1.0.0"
